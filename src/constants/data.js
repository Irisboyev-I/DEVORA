export const topics = [
    {
        emoji: '🔤',
        titleKey: 'topics.jsx.title',
        descKey: 'topics.jsx.desc',
        link: '/articles/jsx',
    },
    {
        emoji: '🧩',
        titleKey: 'topics.components.title',
        descKey: 'topics.components.desc',
        link: '/articles/components',
    },
    {
        emoji: '📦',
        titleKey: 'topics.props.title',
        descKey: 'topics.props.desc',
        link: '/articles/props',
    },
    {
        emoji: '⚙️',
        titleKey: 'topics.usestate.title',
        descKey: 'topics.usestate.desc',
        link: '/articles/usestate',
    },
    {
        emoji: '🔁',
        titleKey: 'topics.useeffect.title',
        descKey: 'topics.useeffect.desc',
        link: '/articles/useeffect',
    },
    {
        emoji: '🧠',
        titleKey: 'topics.usecontext.title',
        descKey: 'topics.usecontext.desc',
        link: '/articles/usecontext',
    },
];

export const articlePreviews = [
    {
        id: 'jsx',
        emoji: '🔤',
        title: 'topics.jsx.title',
        summary: 'topics.jsx.desc',
        link: '/articles/jsx',
    },
    {
        id: 'components',
        emoji: '🧩',
        title: 'topics.components.title',
        summary: 'topics.components.desc',
        link: '/articles/components',
    },
    {
        id: 'props',
        emoji: '📦',
        title: 'topics.props.title',
        summary: 'topics.props.desc',
        link: '/articles/props',
    },
    {
        id: 'usestate',
        emoji: '⚙️',
        title: 'topics.usestate.title',
        summary: 'topics.usestate.desc',
        link: '/articles/usestate',
    },
    {
        id: 'useeffect',
        emoji: '🔁',
        title: 'topics.useeffect.title',
        summary: 'topics.useeffect.desc',
        link: '/articles/useeffect',
    },
    {
        id: 'usecontext',
        emoji: '🧠',
        title: 'topics.usecontext.title',
        summary: 'topics.usecontext.desc',
        link: '/articles/usecontext',
    },
];


export const benefits = [
    { title: 'noSignup' },
    { title: 'free' },
    { title: 'withExamples' },
    { title: 'practicalTips' },
    { title: 'forBeginners' },
];

export const footerData = {
    articles: [
        { key: 'jsx', path: '/articles/jsx' },
        { key: 'components', path: '/articles/components' },
        { key: 'props', path: '/articles/props' },
        { key: 'usestate', path: '/articles/usestate' },
        { key: 'useeffect', path: '/articles/useeffect' },
    ],
    moreArticles: [
        { key: 'useref_usememo', path: '/articles/useref-usememo' },
        { key: 'usecontext', path: '/articles/usecontext' },
        { key: 'customhooks', path: '/articles/customhooks' },
        { key: 'forms', path: '/articles/forms' },
        { key: 'validation', path: '/articles/validationForm' },
        { key: 'api', path: '/articles/api' },
        { key: 'react_query', path: '/articles/react-query' },
        { key: 'rest_graphql', path: '/articles/restAndGraphQL' },
        { key: 'routing', path: '/articles/routing' },
        { key: 'private_routing', path: '/articles/privateRouting' },
        { key: 'tailwind', path: '/articles/tailwind' },
        { key: 'styled_components', path: '/articles/styledComponents' },
        { key: 'conditional_render', path: '/articles/render' },
        { key: 'lifting_state', path: '/articles/lifting-state' },
        { key: 'lazyload', path: '/articles/lazyload' },
        { key: 'more', path: '/articles' },
    ],
    contacts: [
        { type: 'email', text: 'Email: irisboyev.ii@gmail.com' },
        { type: 'telegram', text: 'Telegram: @irisboyevw', href: 'https://t.me/irisboyevw' },
        { type: 'whatsapp', text: 'WhatsApp: +998 20 010 52 21', href: 'https://wa.me/998200105221' },
    ],
};

export const AllArticles = [
    { key: 'jsx', path: '/articles/jsx', descriptionKey: 'articles.jsx.description' },
    { key: 'components', path: '/articles/components', descriptionKey: 'articles.components.description' },
    { key: 'props', path: '/articles/props', descriptionKey: 'articles.props.description' },
    { key: 'usestate', path: '/articles/usestate', descriptionKey: 'articles.usestate.description' },
    { key: 'useeffect', path: '/articles/useeffect', descriptionKey: 'articles.useeffect.description' },
    { key: 'useref_usememo', path: '/articles/useref-usememo', descriptionKey: 'articles.useref_usememo.description' },
    { key: 'usecontext', path: '/articles/usecontext', descriptionKey: 'articles.usecontext.description' },
    { key: 'customhooks', path: '/articles/customhooks', descriptionKey: 'articles.customhooks.description' },
    { key: 'forms', path: '/articles/forms', descriptionKey: 'articles.forms.description' },
    { key: 'validation', path: '/articles/validationForm', descriptionKey: 'articles.validation.description' },
    { key: 'api', path: '/articles/api', descriptionKey: 'articles.api.description' },
    { key: 'react_query', path: '/articles/react-query', descriptionKey: 'articles.react_query.description' },
    { key: 'rest_graphql', path: '/articles/restAndGraphQL', descriptionKey: 'articles.rest_graphql.description' },
    { key: 'routing', path: '/articles/routing', descriptionKey: 'articles.routing.description' },
    { key: 'private_routing', path: '/articles/privateRouting', descriptionKey: 'articles.private_routing.description' },
    { key: 'tailwind', path: '/articles/tailwind', descriptionKey: 'articles.tailwind.description' },
    { key: 'styled_components', path: '/articles/styledComponents', descriptionKey: 'articles.styled_components.description' },
    { key: 'conditional_render', path: '/articles/render', descriptionKey: 'articles.conditional_render.description' },
    { key: 'lifting_state', path: '/articles/lifting-state', descriptionKey: 'articles.lifting_state.description' },
    { key: 'lazyload', path: '/articles/lazyload', descriptionKey: 'articles.lazyload.description' },
];

export const articlesMoreData = [
    {
        key: 'jsx',
        title: {
            ru: 'JSX',
            en: 'JSX',
            uz: 'JSX',
        },
        description: {
            ru: 'JSX — это синтаксическое расширение JavaScript, которое позволяет писать HTML-подобный код внутри React-компонентов.',
            en: 'JSX is a syntax extension for JavaScript that allows you to write HTML-like code inside React components.',
            uz: 'JSX — bu JavaScript uchun sintaksis kengaytmasi bo‘lib, React komponentlarida HTML-ga o‘xshash kod yozishga imkon beradi.',
        },
        content: {
            ru: `JSX позволяет описывать пользовательский интерфейс в декларативной форме, что делает структуру компонентов более наглядной и близкой к привычной HTML-разметке. Под капотом JSX преобразуется в вызовы функции React.createElement, поэтому он остаётся валидным JavaScript-кодом.

JSX поддерживает встраивание JavaScript-выражений внутри фигурных скобок, что позволяет динамически управлять содержимым компонентов. Также поддерживаются вложенные компоненты, условная отрисовка, списки и даже стилизация через объекты.

Важно помнить, что JSX требует строгого соблюдения синтаксиса: все теги должны быть закрыты, атрибуты именуются в camelCase (например, className вместо class), и можно использовать React-фрагменты для группировки элементов без добавления лишнего узла в DOM.`,
            en: `JSX allows you to describe user interfaces declaratively, making the component structure more intuitive and similar to traditional HTML markup. Under the hood, JSX is transformed into React.createElement calls, so it remains valid JavaScript code.

JSX supports embedding JavaScript expressions inside curly braces, enabling dynamic content rendering. It also supports nested components, conditional rendering, lists, and even styling through objects.

It's important to remember that JSX has strict syntax rules: all tags must be closed, attributes use camelCase naming (e.g., className instead of class), and you can use React fragments to group elements without adding extra nodes to the DOM.`,
            uz: `JSX foydalanuvchi interfeysini deklarativ tarzda ta’riflash imkonini beradi, bu esa komponentlar tuzilmasini tushunarli va an’anaviy HTML belgilanishiga o‘xshash qiladi. Ichki jihatdan JSX React.createElement chaqiruvlariga aylantiriladi, shuning uchun u amal qiluvchi JavaScript kodi bo‘lib qoladi.

JSX ichida jingalak qavslar ichida JavaScript ifodalarini joylashtirishni qo‘llab-quvvatlaydi, bu esa komponent mazmunini dinamik boshqarishga imkon beradi. Shuningdek, ichma-ich komponentlar, shartli render qilish, ro‘yxatlar va hatto obyektlar orqali stil berish ham qo‘llaniladi.

Muhimi shundaki, JSX sintaksisiga qat’iy rioya qilish talab etiladi: barcha teglar yopilgan bo‘lishi kerak, atributlar camelCase formatida bo‘ladi (masalan, class o‘rniga className), va React fragmentlari ortiqcha DOM tuguni qo‘shmasdan elementlarni guruhlash uchun ishlatiladi.`,
        },
        exampleJsx: `
import React from 'react';

const UserCard = ({ name, age }) => {
  return (
    <div className="card">
      <h2>Имя: {name}</h2>
      <p>Возраст: {age}</p>
      <button onClick={() => alert(\`Привет, \${name}!\`)}>Поздороваться</button>
    </div>
  );
};

export default UserCard;`,
        exampleTsx: `
import React from 'react';

type UserCardProps = {
  name: string;
  age: number;
};

const UserCard: React.FC<UserCardProps> = ({ name, age }) => {
  return (
    <div className="card">
      <h2>Имя: {name}</h2>
      <p>Возраст: {age}</p>
      <button onClick={() => alert(\`Привет, \${name}!\`)}>Поздороваться</button>
    </div>
  );
};

export default UserCard;`,
        proTips: {
            ru: [
                'JSX всегда должен возвращать один корневой элемент — используй обёртку <div> или фрагменты <>...</>.',
                'Все теги (даже <img /> и <input />) должны быть закрыты — это обязательное требование.',
                'Не используй if/else внутри JSX — вместо этого применяй тернарный оператор или логическое &&.',
                'Ключевое слово class в JSX заменяется на className, а for — на htmlFor.',
                'Старайся избегать сложной логики прямо в JSX — выноси её в отдельные функции или переменные.',
            ],
            en: [
                'JSX must return a single root element — use a <div> wrapper or fragments <>...</>.',
                'All tags (even <img /> and <input />) must be self-closed — this is required.',
                'Avoid using if/else directly in JSX — use ternary operators or logical && instead.',
                'Use className instead of class, and htmlFor instead of for.',
                'Avoid complex logic inside JSX — move it to separate functions or variables.',
            ],
            uz: [
                'JSX har doim bitta asosiy elementni qaytarishi kerak — <div> yoki fragmentlar <>...</> dan foydalaning.',
                'Barcha teglar (hatto <img /> va <input /> ham) yopilgan bo‘lishi kerak — bu majburiy.',
                'JSX ichida if/else dan foydalanmang — buning o‘rniga ternary operatori yoki && mantiqiy operatorini qo‘llang.',
                'class o‘rniga className, for o‘rniga esa htmlFor yozing.',
                'Murakkab mantiqni JSX ichiga kiritmang — uni alohida funksiyalarga yoki o‘zgaruvchilarga ajrating.',
            ],
        }
    },


    {
        key: 'components',
        title: {
            ru: 'Компоненты',
            en: 'Components',
            uz: 'Komponentlar',
        },
        description: {
            ru: 'Компоненты — это строительные блоки React-приложения, позволяющие создавать повторно используемые, изолированные элементы интерфейса.',
            en: 'Components are the building blocks of React applications, allowing you to create reusable and isolated UI elements.',
            uz: 'Komponentlar — bu React ilovalarining asosiy qismlari bo‘lib, qayta ishlatiladigan va izolyatsiyalangan interfeys elementlarini yaratish imkonini beradi.',
        },
        content: {
            ru: `Компоненты — это функции (или классы), которые возвращают JSX. Они позволяют разбивать интерфейс на небольшие части, каждая из которых отвечает за свою логику и отображение.

Существует два типа компонентов:
- **Функциональные компоненты** — наиболее распространённый способ, используют обычные функции. С React Hooks они могут управлять состоянием и побочными эффектами.
- **Классовые компоненты** — раньше использовались для управления состоянием и жизненным циклом, но сейчас их вытесняют функциональные.

Компоненты могут принимать **props** — внешние параметры, передаваемые при вызове, и использовать **state** — внутренние данные компонента.

Компоненты делают код более модульным, читаемым и легко поддерживаемым. Они могут быть вложенными друг в друга и объединяться в более сложные структуры.`,
            en: `Components are functions (or classes) that return JSX. They allow you to split the interface into small parts, each responsible for its own logic and rendering.

There are two types of components:
- **Functional components** — the most common type, using standard functions. With React Hooks, they can handle state and side effects.
- **Class components** — previously used to manage state and lifecycle, now largely replaced by functional ones.

Components can receive **props** — external parameters passed from the parent, and manage **state** — internal data.

They make the code more modular, readable, and maintainable. Components can be nested and combined into more complex structures.`,
            uz: `Komponentlar — bu JSX qaytaradigan funksiyalar (yoki klasslar). Ular interfeysni kichik qismlarga bo‘lishga imkon beradi, har biri o‘z logikasi va ko‘rinishi uchun javob beradi.

Komponentlarning ikki turi mavjud:
- **Funktsional komponentlar** — eng keng tarqalgan turi bo‘lib, oddiy funksiyalar asosida ishlaydi. React Hooks yordamida ular holat va effektlarni boshqarishi mumkin.
- **Klass komponentlar** — avval holat va lifecycle uchun ishlatilgan, lekin hozirda funksional komponentlar bilan almashtirilmoqda.

Komponentlar **props** (tashqi parametrlar) va **state** (ichki maʼlumotlar) bilan ishlaydi.

Komponentlar kodni modulli, o‘qiladigan va oson qo‘llab-quvvatlanadigan qiladi. Ular ichma-ich joylashgan bo‘lishi va murakkab tuzilmalarga birlashishi mumkin.`,
        },
        exampleJsx: `
import React from 'react';

// Компонент кнопки с текстом, переданным через props
function CustomButton({ label, onClick }) {
  return (
    <button onClick={onClick} className="btn">
      {label}
    </button>
  );
}

// Использование компонента
export default function App() {
  return (
    <div>
      <CustomButton label="Нажми меня" onClick={() => alert('Привет!')} />
    </div>
  );
}
`,
        exampleTsx: `
import React from 'react';

type CustomButtonProps = {
  label: string;
  onClick: () => void;
};

const CustomButton: React.FC<CustomButtonProps> = ({ label, onClick }) => {
  return (
    <button onClick={onClick} className="btn">
      {label}
    </button>
  );
};

export default function App() {
  return (
    <div>
      <CustomButton label="Нажми меня" onClick={() => alert('Привет!')} />
    </div>
  );
}
`,
        proTips: {
            ru: [
                'Имя компонента всегда должно начинаться с заглавной буквы (PascalCase), иначе React воспримет его как обычный HTML-тег.',
                'Хорошая практика — один компонент = один файл, чтобы структура проекта была понятной.',
                'Старайся делать компоненты как можно более переиспользуемыми и изолированными от внешнего контекста.',
                'Избегай слишком большого количества логики в компоненте — выноси обработчики и расчёты в отдельные функции.',
                'Для сложных компонентов используй хуки, контексты или кастомные хуки, чтобы управлять логикой более грамотно.',
            ],
            en: [
                'Component names must start with an uppercase letter (PascalCase), otherwise React treats them as HTML tags.',
                'Best practice: one component = one file, for clearer project structure.',
                'Keep components reusable and isolated from external context.',
                'Avoid too much logic in components — move handlers to separate functions.',
                'Use hooks, contexts, or custom hooks to manage logic in complex components.',
            ],
            uz: [
                'Komponent nomlari har doim katta harf bilan boshlanishi kerak (PascalCase), aks holda React uni HTML teg deb qabul qiladi.',
                'Yaxshi amaliyot — har bir komponent alohida faylda bo‘lishi.',
                'Komponentlar iloji boricha qayta ishlatiladigan va tashqi kontekstdan mustaqil bo‘lishi kerak.',
                'Murakkab hisob-kitoblarni alohida funksiyalarga ajrating.',
                'Murakkab komponentlar uchun hooklar, kontekstlar yoki custom hooklardan foydalaning.',
            ]
        }
    },


    {
        key: 'props',
        title: {
            ru: 'Пропсы',
            en: 'Props',
            uz: 'Propslar',
        },
        description: {
            ru: 'Пропсы (props) — это параметры, передаваемые в компонент из родительского компонента. Они делают компонент динамическим и переиспользуемым.',
            en: 'Props are parameters passed from a parent to a component. They make components dynamic and reusable.',
            uz: 'Propslar — bu komponentga ota-komponentdan uzatiladigan parametrlar. Ular komponentlarni dinamik va qayta ishlatiladigan qiladi.',
        },
        content: {
            ru: `Слово "props" — сокращение от "properties" (свойства). Это объект, который React передаёт каждому компоненту, содержащий все переданные значения.

Пропсы позволяют компоненту быть универсальным: он может отображать разную информацию, основываясь на полученных данных.

Компонент **не должен изменять свои пропсы** — они только для чтения. Если нужно что-то изменить, это должно происходить в родителе (через state).

Пропсы можно использовать:
- для передачи текста, чисел, функций и JSX,
- для управления отображением (например, показывать/скрывать),
- для обратной связи через callback-функции.`,
            en: `"Props" is short for "properties". It’s an object that React passes to components, containing all received values.

Props allow components to be reusable by rendering different content based on the passed data.

Components **should not change their own props** — they are read-only. Any change must happen in the parent (via state).

You can use props:
- to pass text, numbers, functions, or JSX,
- to control rendering (e.g. show/hide),
- for callbacks from child to parent.`,
            uz: `"Props" — bu "properties" (xususiyatlar) so‘zining qisqartmasi. React har bir komponentga bu obyektni uzatadi, u orqali barcha qiymatlar kiradi.

Propslar komponentlarni qayta ishlatiladigan qiladi: ular kiritilgan maʼlumotga qarab turlicha ko‘rinishni taʼminlaydi.

Komponent **o‘z propslarini o‘zgartirmasligi kerak** — ular faqat o‘qish uchun. O‘zgarishlar ota-komponentda state orqali bajariladi.

Propslar quyidagilar uchun ishlatiladi:
- matn, raqam, funksiya yoki JSX uzatish,
- ko‘rinishni boshqarish (masalan, ko‘rsatish/yashirish),
- child → parent callbacklar uchun.`,
        },
        exampleJsx: `
import React from 'react';

// Компонент, принимающий заголовок и описание через props
function InfoCard({ title, description }) {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
}

// Родительский компонент, передающий данные
export default function App() {
  return (
    <InfoCard
      title="О React"
      description="React — это библиотека для создания пользовательских интерфейсов."
    />
  );
}
`,
        exampleTsx: `
import React from 'react';

type InfoCardProps = {
  title: string;
  description: string;
};

const InfoCard: React.FC<InfoCardProps> = ({ title, description }) => {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
};

export default function App() {
  return (
    <InfoCard
      title="О React"
      description="React — это библиотека для создания пользовательских интерфейсов."
    />
  );
}
`,
        proTips: {
            ru: [
                'Передавай только нужные props — чем меньше, тем проще компонент.',
                'Используй деструктуризацию для чистоты и читаемости кода: `{ title, desc }` вместо `props.title`.',
                'Передавай callback-функции через props, чтобы дочерний компонент мог общаться с родителем.',
                'Используй TypeScript (или PropTypes в JS), чтобы проверять типы props и избежать ошибок.',
                'Если компоненту нужно значение "по умолчанию", используй `defaultProps` или значения по умолчанию в параметрах.',
            ],
            en: [
                'Pass only the necessary props — the fewer, the simpler the component.',
                'Use destructuring for cleaner code: `{ title, desc }` instead of `props.title`.',
                'Pass callback functions via props so child components can communicate with parents.',
                'Use TypeScript or PropTypes to validate props and avoid bugs.',
                'Use defaultProps or parameter defaults for default values.',
            ],
            uz: [
                'Faqat kerakli propslarni uzat — qancha kam bo‘lsa, komponent shunchalik sodda bo‘ladi.',
                'Kod tozaligi uchun destrukturizatsiyadan foydalan: `{ title, desc }` o‘rniga `props.title`.',
                'Callback funksiyalarni props orqali uzatib, child → parent aloqa o‘rnat.',
                'Propslar turlarini tekshirish uchun TypeScript yoki PropTypes ishlat.',
                'Agar komponentga default qiymat kerak bo‘lsa, `defaultProps` yoki parametr default qiymatidan foydalan.',
            ]
        }
    },


    {
        key: 'usestate',
        title: {
            ru: 'useState',
            en: 'useState',
            uz: 'useState',
        },
        description: {
            ru: 'Хук useState позволяет компонентам React управлять локальным состоянием — хранить и изменять данные при взаимодействии с пользователем.',
            en: 'The useState hook lets React components manage local state — storing and updating data when users interact.',
            uz: 'useState hook React komponentlariga foydalanuvchi bilan o‘zaro aloqada bo‘lganda lokal holatni boshqarish — saqlash va o‘zgartirish imkonini beradi.',
        },
        content: {
            ru: `React-компоненты по умолчанию — чистые функции. Чтобы хранить изменяемые данные (например, счётчик, форму, флаг показа), используется хук \`useState\`.

Он возвращает массив из двух элементов:
1. текущее значение состояния,
2. функция, которая позволяет изменить это значение.

Изменение состояния вызывает повторный рендер компонента.

Синтаксис:
\`\`\`js
const [value, setValue] = useState(initialValue);
\`\`\`

Где \`initialValue\` — начальное значение, а \`setValue\` — функция для его изменения.`,
            en: `React components are pure functions by default. To store changeable data (like counters, forms, flags), use the \`useState\` hook.

It returns an array with two elements:
1. the current state value,
2. a function to update that value.

Changing state triggers re-rendering.

Syntax:
\`\`\`js
const [value, setValue] = useState(initialValue);
\`\`\`

Where \`initialValue\` is the default value, and \`setValue\` updates it.`,
            uz: `React komponentlari standart bo‘lib “pok funksiyalar”dir. O‘zgaruvchan maʼlumotlarni (masalan, hisoblagich, forma, bayroq) saqlash uchun \`useState\` hookidan foydalaniladi.

U ikkita elementdan iborat massivni qaytaradi:
1. holatning joriy qiymati,
2. uni o‘zgartirish funksiyasi.

Holat o‘zgarganda komponent qayta render qilinadi.

Sintaksis:
\`\`\`js
const [value, setValue] = useState(initialValue);
\`\`\`

Bu yerda \`initialValue\` — boshlang‘ich qiymat, \`setValue\` — uni yangilovchi funksiya.`,
        },
        exampleJsx: `
import React, { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0); // начальное значение — 0

  const increment = () => {
    setCount(count + 1); // обновляем состояние
  };

  return (
    <div>
      <p>Счётчик: {count}</p>
      <button onClick={increment}>Увеличить</button>
    </div>
  );
}

export default Counter;
`,
        exampleTsx: `
import React, { useState } from 'react';

export default function Counter(): JSX.Element {
  const [count, setCount] = useState<number>(0);

  return (
    <div>
      <p>Счётчик: {count}</p>
      <button onClick={() => setCount(count + 1)}>Увеличить</button>
    </div>
  );
}
`,
        proTips: {
            ru: [
                'Никогда не изменяй состояние напрямую: нельзя делать `count++`, всегда используй `setCount(count + 1)`.',
                'Если новое значение зависит от старого, используй функцию: `setCount(prev => prev + 1)`.',
                'Можно хранить не только числа, но и строки, булевы значения, объекты и массивы.',
                'Состояние сбрасывается при размонтировании компонента — для глобального хранения используй context или state-менеджеры.',
                'Если нужно хранить несколько значений, вызывай `useState` несколько раз — один хук = одно значение.',
            ],
            en: [
                'Never change state directly — avoid `count++`, always use `setCount(count + 1)`.',
                'When the new value depends on the old one, use a function: `setCount(prev => prev + 1)`.',
                'You can store not just numbers, but strings, booleans, objects, and arrays.',
                'State resets when the component unmounts — use context or state managers for global data.',
                'Use `useState` multiple times to manage multiple values — one hook = one piece of state.',
            ],
            uz: [
                'Holatni hech qachon to‘g‘ridan-to‘g‘ri o‘zgartirmang — `count++` emas, `setCount(count + 1)` ishlating.',
                'Agar yangi qiymat eski qiymatga bog‘liq bo‘lsa, funksiya ko‘rinishida yozing: `setCount(prev => prev + 1)`.',
                'Faqat raqam emas, balki string, boolean, obyekt va massivlarni ham saqlashingiz mumkin.',
                'Komponent unmount bo‘lganda holat yo‘qoladi — umumiy holat uchun context yoki state manager ishlating.',
                'Ko‘p holatlarni saqlash uchun `useState`ni bir necha marta chaqiring — bir hook = bir qiymat.',
            ],
        }
    },


    {
        key: 'useeffect',
        title: {
            ru: 'useEffect',
            en: 'useEffect',
            uz: 'useEffect',
        },
        description: {
            ru: 'Хук useEffect позволяет выполнять побочные эффекты в компонентах React: запросы к API, подписки, таймеры, и синхронизацию с внешними системами.',
            en: 'The useEffect hook is used for handling side effects in React components: API calls, subscriptions, timers, and syncing with external systems.',
            uz: 'useEffect hook React komponentlarida yon ta’sirlarni bajarish uchun ishlatiladi: API chaqiruvlari, obunalar, taymerlar va tashqi tizimlar bilan sinxronlash.',
        },
        content: {
            ru: `React по своей природе «чистый» — он не делает ничего, кроме рендера. Но в реальных приложениях нам нужно:

- получать данные с сервера (fetch),
- изменять заголовок страницы,
- запускать таймеры,
- подписываться на события (например, window resize),
- взаимодействовать с локальным хранилищем и др.

Для этого существует хук useEffect.

Синтаксис:
  useEffect(() => {
    // побочный эффект
    return () => {
      // очистка эффекта (необязательно)
    }
  }, [зависимости]);

- Первая функция — сам эффект, вызывается после рендера.
- Вторая часть — массив зависимостей, от которых зависит повторный запуск эффекта.
- Возвращаемая функция (опционально) используется для очистки (аналог componentWillUnmount).`,
            en: `React by nature is “pure” — it only renders. But in real-world apps we need to:

- fetch data from server,
- update the document title,
- run timers,
- subscribe to events (like window resize),
- interact with local storage, etc.

That’s what useEffect is for.

Syntax:
  useEffect(() => {
    // effect logic
    return () => {
      // cleanup (optional)
    }
  }, [dependencies]);

- First function runs after render.
- Second parameter is dependency array to re-run effect.
- The returned function is for cleanup (like componentWillUnmount).`,
            uz: `React tabiatan “pok” — u faqat render qiladi. Lekin haqiqiy ilovalarda bizga kerak bo‘ladi:

- serverdan ma’lumot olish (fetch),
- sahifa sarlavhasini o‘zgartirish,
- taymerlar ishga tushirish,
- voqealarga obuna bo‘lish (masalan, window resize),
- localStorage bilan ishlash va boshqalar.

Buning uchun useEffect mavjud.

Sintaksis:
  useEffect(() => {
    // yon ta’sir
    return () => {
      // tozalash (majburiy emas)
    }
  }, [bog‘liqliklar]);

- Birinchi funksiya — effektning o‘zi, renderdan keyin chaqiriladi.
- Ikkinchi parametr — qaysi qiymat o‘zgarganda qayta ishlash kerakligi.
- Qaytarilgan funksiya tozalash uchun ishlatiladi (componentWillUnmount o‘xshash).`,
        },
        exampleJsx: `
import React, { useState, useEffect } from 'react';

function Timer() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      setCount(prev => prev + 1);
    }, 1000);

    return () => clearInterval(interval); // очистка при размонтировании
  }, []); // [] — эффект сработает один раз при монтировании

  return <p>Секунд прошло: {count}</p>;
}

export default Timer;
`,
        exampleTsx: `
import React, { useState, useEffect } from 'react';

export default function Timer(): JSX.Element {
  const [count, setCount] = useState<number>(0);

  useEffect(() => {
    const interval = setInterval(() => {
      setCount(prev => prev + 1);
    }, 1000);

    return () => clearInterval(interval); // очистка
  }, []);

  return <p>Секунд прошло: {count}</p>;
}
`,
        proTips: {
            ru: [
                'Передавай в массив зависимостей ВСЁ, что используется внутри эффекта (переменные, функции). Это предотвращает баги.',
                'Эффект без зависимостей ([] срабатывает один раз) — как componentDidMount.',
                'Без массива зависимостей useEffect срабатывает после каждого рендера.',
                'Если указать зависимость, например [user], эффект будет срабатывать при её изменении.',
                'Не вызывай async напрямую — оберни в async-функцию внутри useEffect.',
                'Очищай таймеры и подписки — это предотвращает утечки памяти.',
            ],
            en: [
                'Include EVERYTHING used inside effect in the dependencies array to avoid bugs.',
                'Effect with empty deps (`[]`) runs once — like componentDidMount.',
                'Without deps array, useEffect runs after every render.',
                'With [user] as a dependency, effect reruns when user changes.',
                'Do not call async directly — wrap it in an inner async function.',
                'Clean up timers and subscriptions to prevent memory leaks.',
            ],
            uz: [
                'Effekt ichida ishlatilgan BARCHA qiymatlarni dependenciesga yozing — bu xatoliklardan saqlaydi.',
                'Bo‘sh deps bilan (`[]`) — effekt faqat bir marta ishlaydi.',
                'Agar deps yo‘q bo‘lsa, useEffect har renderdan keyin ishlaydi.',
                '[user] — bog‘liq qiymat bo‘lsa, o‘zgarganida effekt ishlaydi.',
                'async funksiyani bevosita chaqirmang — uni ichki async funksiyaga o‘rang.',
                'Taymer va obunalarni tozalash — xotira oqishlarining oldini oladi.',
            ]
        }
    },


    {
        key: 'useref-usememo',
        title: {
            ru: 'useRef и useMemo',
            en: 'useRef and useMemo',
            uz: 'useRef va useMemo',
        },
        description: {
            ru: 'Хуки useRef и useMemo позволяют управлять ссылками и мемоизацией в React-компонентах для повышения производительности.',
            en: 'The useRef and useMemo hooks allow you to manage references and memoization in React components to improve performance.',
            uz: 'useRef va useMemo hooklari React komponentlarida havolalarni va memoizatsiyani boshqarishga yordam beradi, bu esa unumdorlikni oshiradi.',
        },
        content: {
            ru: `🔹 useRef:
Хук useRef возвращает изменяемый объект с .current. Он часто используется для получения доступа к DOM-элементу напрямую или хранения значения, которое сохраняется между рендерами, но не вызывает повторный рендер.

🔹 useMemo:
Хук useMemo мемоизирует результат вычислений. Это полезно, если у вас есть дорогая вычислительная функция и вы не хотите, чтобы она выполнялась при каждом рендере без нужды.

Оба хука помогают в оптимизации и контроле над производительностью, особенно в сложных и тяжёлых компонентах.`,
            en: `🔹 useRef:
The useRef hook returns a mutable object with a .current property. It’s commonly used to access DOM elements directly or store a value that persists across renders without causing re-renders.

🔹 useMemo:
The useMemo hook memoizes the result of a computation. It’s useful for expensive calculations that shouldn’t re-run on every render.

Both hooks help optimize and control performance, especially in heavy components.`,
            uz: `🔹 useRef:
useRef hook .current maydoniga ega bo‘lgan o‘zgaruvchan obyektni qaytaradi. U ko‘pincha DOM elementlarga to‘g‘ridan-to‘g‘ri kirish yoki renderlar orasida o‘zgarmas qiymatlarni saqlash uchun ishlatiladi.

🔹 useMemo:
useMemo hook hisoblash natijalarini memoizatsiya qiladi. Bu ayniqsa murakkab hisoblashlar har safar renderda qayta ishlashini oldini olish uchun foydalidir.

Ikkala hook ham katta va murakkab komponentlarda samaradorlikni oshirish uchun ishlatiladi.`,
        },
        exampleJsx: `
import React, { useRef, useMemo } from 'react';

const ExpensiveComponent = ({ items }) => {
  const renderCount = useRef(0);
  renderCount.current++;

  const total = useMemo(() => {
    console.log('Calculating total...');
    return items.reduce((acc, item) => acc + item.value, 0);
  }, [items]);

  return (
    <div>
      <p>Итог: {total}</p>
      <p>Компонент отрендерен: {renderCount.current} раз(а)</p>
    </div>
  );
};

export default ExpensiveComponent;
`,
        exampleTsx: `
import React, { useRef, useMemo } from 'react';

interface Item {
  value: number;
}

interface Props {
  items: Item[];
}

const ExpensiveComponent: React.FC<Props> = ({ items }) => {
  const renderCount = useRef(0);
  renderCount.current++;

  const total = useMemo(() => {
    console.log('Calculating total...');
    return items.reduce((acc, item) => acc + item.value, 0);
  }, [items]);

  return (
    <div>
      <p>Итог: {total}</p>
      <p>Компонент отрендерен: {renderCount.current} раз(а)</p>
    </div>
  );
};

export default ExpensiveComponent;
`,
        proTips: {
            ru: [
                'useRef можно использовать как альтернативу useState для значений, которые не влияют на рендер.',
                'useMemo применяй только там, где действительно есть дорогие вычисления.',
                'Следи за зависимостями в массиве useMemo, чтобы избежать неожиданных багов.',
            ],
            en: [
                'useRef can be used as an alternative to useState for values that don’t affect rendering.',
                'Only use useMemo when you have expensive computations.',
                'Always check the dependency array in useMemo to avoid unexpected bugs.',
            ],
            uz: [
                'useRef ni useState o‘rniga foydalanish mumkin — agar qiymat renderga taʼsir qilmasa.',
                'useMemo faqat murakkab hisoblashlar uchun ishlating.',
                'useMemo dependency array ni to‘g‘ri belgilash muhim — noto‘g‘ri holatda xatoliklar yuzaga keladi.',
            ],
        }
    },


    {
        key: 'usecontext',
        title: {
            ru: 'useContext',
            en: 'useContext',
            uz: 'useContext',
        },
        description: {
            ru: 'Хук useContext позволяет использовать глобальные значения в React без необходимости пробрасывать пропсы вручную через каждый компонент.',
            en: 'The useContext hook allows accessing global values in React without manually passing props through each component.',
            uz: 'useContext hooki Reactda global qiymatlarni qo‘lda props orqali uzatmasdan olish imkonini beradi.',
        },
        content: {
            ru: `🔹 useContext:
Хук useContext позволяет извлекать значения из контекста, созданного через React.createContext. Это особенно полезно, когда нужно передать данные (например, тему, язык или текущего пользователя) глубоко в дерево компонентов без пробрасывания пропсов на каждом уровне.

Контекст содержит два элемента:
1. Провайдер (Provider) — оборачивает компонент и предоставляет значение.
2. Получатель (Consumer или useContext) — получает значение из контекста.

Контекст — это альтернатива prop drilling и может сделать код более чистым и масштабируемым.`,
            en: `🔹 useContext:
The useContext hook lets you access values from a context created using React.createContext. It’s especially useful when you need to provide data like theme, language, or user deep in the component tree without prop drilling.

Context includes two parts:
1. Provider — wraps a component and supplies the value.
2. Consumer (or useContext) — receives the value.

Context helps avoid prop drilling and makes the code cleaner and more scalable.`,
            uz: `🔹 useContext:
useContext hooki React.createContext orqali yaratilgan kontekstdan qiymatlarni olish imkonini beradi. Bu, masalan, tema, til yoki foydalanuvchini chuqur komponent daraxtiga uzatishda props orqali uzatishni oldini oladi.

Context ikkita asosiy qismlardan iborat:
1. Provider — komponentni o‘rab oladi va qiymat beradi.
2. Consumer yoki useContext — qiymatni oladi.

Context prop drilling o‘rnini bosuvchi yechim bo‘lib, kodni soddaroq va kengaytiriladigan qiladi.`,
        },
        exampleJsx: `
import React, { createContext, useContext } from 'react';

// 1. Создаём контекст
const ThemeContext = createContext('light');

const ThemedComponent = () => {
  const theme = useContext(ThemeContext);
  return <div>Текущая тема: {theme}</div>;
};

const App = () => {
  return (
    <ThemeContext.Provider value="dark">
      <ThemedComponent />
    </ThemeContext.Provider>
  );
};

export default App;
`,
        exampleTsx: `
import React, { createContext, useContext } from 'react';

// Создаём типы
type Theme = 'light' | 'dark';
const ThemeContext = createContext<Theme>('light');

const ThemedComponent: React.FC = () => {
  const theme = useContext(ThemeContext);
  return <div>Текущая тема: {theme}</div>;
};

const App: React.FC = () => {
  return (
    <ThemeContext.Provider value="dark">
      <ThemedComponent />
    </ThemeContext.Provider>
  );
};

export default App;
`,
        proTips: {
            ru: [
                'Контекст — не глобальный стейт. Используй его только для общих значений (тема, язык, пользователь).',
                'Не помещай часто изменяющееся состояние в Context — это может вызывать лишние перерисовки.',
                'Контексты можно разбивать по смыслу (например, отдельный ThemeContext и AuthContext).',
            ],
            en: [
                'Context is not global state — use it only for shared values like theme, language, or user.',
                'Avoid storing rapidly changing data in context — it can trigger unnecessary re-renders.',
                'Split contexts by purpose (e.g., ThemeContext and AuthContext).',
            ],
            uz: [
                'Context — bu global state emas, faqat umumiy qiymatlar uchun foydalaning (masalan, tema, til, foydalanuvchi).',
                'Tez-tez o‘zgaradigan qiymatlarni Context ichida saqlamang — bu keraksiz qayta chizishlarga olib keladi.',
                'Contextlarni maqsadiga qarab ajrating (masalan, ThemeContext va AuthContext).',
            ],
        }
    },


    {
        key: 'customhooks',
        title: {
            ru: 'Custom Hooks',
            en: 'Custom Hooks',
            uz: 'Custom Hooklar',
        },
        description: {
            ru: 'Пользовательские хуки (Custom Hooks) позволяют переиспользовать логику между компонентами, сохраняя код чистым и читаемым.',
            en: 'Custom Hooks let you reuse logic between components while keeping the code clean and readable.',
            uz: 'Custom Hooklar komponentlar orasida logikani qayta ishlatishga yordam beradi va kodni aniq hamda tushunarli qiladi.',
        },
        content: {
            ru: `🔹 Custom Hooks (Пользовательские хуки):
Custom Hooks — это функции, начинающиеся с префикса \`use\`, в которых можно использовать другие хуки и инкапсулировать повторяющуюся логику.

Преимущества:
- Повторное использование логики
- Упрощение компонентов
- Централизация логики (например, работы с формами, API, событиями)

Custom Hook — обычная функция, но внутри неё можно вызывать useState, useEffect и другие хуки. Главное правило: имя должно начинаться с "use" и он должен вызываться только в компоненте или другом хуке.`,
            en: `🔹 Custom Hooks:
Custom Hooks are functions starting with the prefix \`use\` that allow using other hooks and encapsulating reusable logic.

Benefits:
- Reuse logic
- Simplify components
- Centralize behavior (forms, APIs, events)

They are just functions that use hooks like useState or useEffect. Rule: the name must start with "use" and be called only inside a hook or a component.`,
            uz: `🔹 Custom Hooklar:
Custom Hooklar \`use\` bilan boshlanuvchi funksiyalar bo‘lib, ichida boshqa hooklarni chaqirib, qayta ishlatiladigan logikani ajratishga imkon beradi.

Afzalliklari:
- Logikani qayta ishlatish
- Komponentlarni soddalashtirish
- Markazlashgan ishlov (formalar, API, hodisalar)

Custom hooklar oddiy funksiya, lekin ichida useState, useEffect kabi hooklardan foydalaniladi. Muhim: nomi "use" bilan boshlanishi va faqat komponent yoki hook ichida chaqirilishi kerak.`,
        },
        exampleJsx: `// useWindowWidth.js
import { useState, useEffect } from 'react';

function useWindowWidth() {
  const [width, setWidth] = useState(window.innerWidth);

  useEffect(() => {
    const handleResize = () => setWidth(window.innerWidth);
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return width;
}

// App.jsx
import React from 'react';
import useWindowWidth from './useWindowWidth';

function App() {
  const width = useWindowWidth();
  return <div>Текущая ширина окна: {width}px</div>;
}

export default App;`,
        exampleTsx: `// useWindowWidth.ts
import { useState, useEffect } from 'react';

function useWindowWidth(): number {
  const [width, setWidth] = useState<number>(window.innerWidth);

  useEffect(() => {
    const handleResize = () => setWidth(window.innerWidth);
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return width;
}

export default useWindowWidth;

// App.tsx
import React from 'react';
import useWindowWidth from './useWindowWidth';

const App: React.FC = () => {
  const width = useWindowWidth();
  return <div>Ширина экрана: {width}px</div>;
};

export default App;`,
        proTips: {
            ru: [
                'Custom Hooks можно использовать для логики формы, API-запросов, взаимодействия с браузером и т. д.',
                'Разделяй хуки по назначению. Один хук — одна задача.',
                'Custom Hook должен следовать правилам хуков: вызываться только внутри компонента или другого хука.',
            ],
            en: [
                'Use custom hooks for form logic, API calls, browser interaction, etc.',
                'Keep hooks focused — one task per hook.',
                'Custom Hooks must follow hook rules: call them only inside components or other hooks.',
            ],
            uz: [
                'Custom Hooklar forma, API so‘rovlari yoki brauzer bilan o‘zaro ta’sir uchun ishlatiladi.',
                'Har bir hook faqat bitta vazifaga xizmat qilsin.',
                'Custom Hooklar faqat komponent yoki boshqa hook ichida chaqirilishi kerak.',
            ],
        }
    },


    {
        key: 'forms',
        title: {
            ru: 'Формы в React',
            en: 'Forms in React',
            uz: 'Reactda formalar',
        },
        description: {
            ru: 'Формы позволяют собирать данные от пользователя и обрабатывать их в компонентах React.',
            en: 'Forms allow you to collect user input and process it inside React components.',
            uz: 'Formalar foydalanuvchi maʼlumotlarini yigʻish va ularni React komponentlarida qayta ishlash imkonini beradi.',
        },
        content: {
            ru: `В React формы управляются с помощью состояния. Это значит, что каждый ввод пользователя отслеживается через useState или другие методы. Это даёт полный контроль над поведением полей и позволяет валидировать ввод, изменять его и реагировать на события формы.

Управляемые компоненты — это поля формы, значение которых задаётся через состояние. Это делает код более предсказуемым и расширяемым. Также можно использовать библиотеки, такие как React Hook Form или Formik, для более удобной работы с формами.`,
            en: `In React, forms are controlled through state. Each input from the user is tracked using useState or similar methods. This gives full control over field behavior and allows validation, transformation, and reacting to form events.

Controlled components are those whose value is set through state. It makes the code predictable and extensible. Libraries like React Hook Form or Formik can simplify working with complex forms.`,
            uz: `Reactda formalar holat (state) orqali boshqariladi. Har bir foydalanuvchi kiritgan qiymat useState yoki boshqa metodlar bilan kuzatiladi. Bu sizga maydonlarning xatti-harakatini to‘liq nazorat qilish, tekshirish va javob berish imkonini beradi.

Boshqariladigan komponentlar — bu qiymati state orqali belgilangan formalar. Kodni tushunarli va kengaytiriladigan qiladi. Murakkab formalar uchun React Hook Form yoki Formik kabi kutubxonalar qulaylik yaratadi.`,
        },
        exampleJsx: `
import React, { useState } from 'react';

function ContactForm() {
  const [name, setName] = useState('');

  const handleSubmit = (e) => {
    e.preventDefault();
    alert(\`Отправлено имя: \${name}\`);
  };

  return (
    <form onSubmit={handleSubmit}>
      <label>
        Имя:
        <input type="text" value={name} onChange={(e) => setName(e.target.value)} />
      </label>
      <button type="submit">Отправить</button>
    </form>
  );
}

export default ContactForm;`,
        exampleTsx: `
import React, { useState } from 'react';

const ContactForm: React.FC = () => {
  const [name, setName] = useState<string>('');

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    alert(\`Отправлено имя: \${name}\`);
  };

  return (
    <form onSubmit={handleSubmit}>
      <label>
        Имя:
        <input type="text" value={name} onChange={(e) => setName(e.target.value)} />
      </label>
      <button type="submit">Отправить</button>
    </form>
  );
};

export default ContactForm;`,
        proTips: {
            ru: [
                'Используй useState для управления значениями формы.',
                'Не забывай вызывать e.preventDefault() при отправке формы.',
                'Разделяй логику формы и отображение, особенно в больших проектах.',
            ],
            en: [
                'Use useState to control form inputs.',
                'Always call e.preventDefault() when submitting a form.',
                'Separate logic and view in large forms for maintainability.',
            ],
            uz: [
                'Forma qiymatlarini boshqarish uchun useState ishlating.',
                'Formani jo‘natishda e.preventDefault() ni unutmang.',
                'Katta loyihalarda forma logikasini alohida ajrating.',
            ],
        }
    },


    {
        key: 'validationForm',
        title: {
            ru: 'Валидация форм в React',
            en: 'Form Validation in React',
            uz: 'Reactda forma validatsiyasi',
        },
        description: {
            ru: 'Валидация форм позволяет проверять правильность данных до их отправки.',
            en: 'Form validation lets you check data correctness before submission.',
            uz: 'Forma validatsiyasi — foydalanuvchi maʼlumotlarini yuborishdan oldin tekshirish usuli.',
        },
        content: {
            ru: `Валидация в формах необходима для проверки корректности введённых данных. Это помогает избежать ошибок и повысить удобство пользователя.

Существует два типа валидации: ручная (написанная вручную логика в компоненте) и с использованием библиотек, таких как React Hook Form, Yup, Formik и других. Ручная валидация даёт полный контроль, но требует больше кода. Библиотеки упрощают процесс и часто включают интеграцию с Yup для схем валидации.`,
            en: `Validation in forms is needed to ensure the data entered is correct. It helps prevent errors and improves user experience.

There are two types of validation: manual (custom logic written inside the component) and library-based (like React Hook Form, Yup, Formik, etc.). Manual gives full control but requires more code. Libraries simplify the process and often integrate with Yup.`,
            uz: `Formadagi validatsiya foydalanuvchi tomonidan kiritilgan maʼlumotlarning to‘g‘riligini tekshirish uchun kerak. Bu xatoliklarni kamaytiradi va foydalanuvchi tajribasini oshiradi.

Validatsiya ikki xil bo‘ladi: qo‘lda yoziladigan (komponent ichida o‘z logikasi bilan) va kutubxona asosida (React Hook Form, Yup, Formik). Kutubxonalar soddalashtirilgan va Yup bilan ishlashga mos.`,
        },
        exampleJsx: `
import React, { useState } from 'react';

function EmailForm() {
  const [email, setEmail] = useState('');
  const [error, setError] = useState('');

  const validateEmail = (email) => {
    return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email);
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (!validateEmail(email)) {
      setError('Некорректный email');
    } else {
      setError('');
      alert('Email корректный!');
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        placeholder="Введите email"
      />
      {error && <p style={{ color: 'red' }}>{error}</p>}
      <button type="submit">Проверить</button>
    </form>
  );
}

export default EmailForm;`,
        exampleTsx: `
import React, { useState } from 'react';

const EmailForm: React.FC = () => {
  const [email, setEmail] = useState<string>('');
  const [error, setError] = useState<string>('');

  const validateEmail = (email: string): boolean => {
    return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email);
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!validateEmail(email)) {
      setError('Некорректный email');
    } else {
      setError('');
      alert('Email корректный!');
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        placeholder="Введите email"
      />
      {error && <p style={{ color: 'red' }}>{error}</p>}
      <button type="submit">Проверить</button>
    </form>
  );
};

export default EmailForm;`,
        proTips: {
            ru: [
                'Добавляй простую валидацию вручную для маленьких форм.',
                'Для сложной логики используй Yup и React Hook Form.',
                'Показывай пользователю ошибки сразу, чтобы улучшить UX.',
            ],
            en: [
                'Use manual validation for small forms.',
                'Use Yup and React Hook Form for complex logic.',
                'Display errors immediately to improve UX.',
            ],
            uz: [
                'Kichik formalar uchun oddiy validatsiyani qo‘lda yozing.',
                'Murakkab holatlar uchun Yup va React Hook Form ishlating.',
                'Xatoliklarni darhol ko‘rsating — bu foydalanuvchiga qulay.',
            ],
        }
    }
    ,


    {
        key: 'api',
        title: {
            ru: 'Работа с API в React',
            en: 'Working with APIs in React',
            uz: 'Reactda API bilan ishlash',
        },
        description: {
            ru: 'Работа с API позволяет получать или отправлять данные между клиентом и сервером.',
            en: 'APIs allow you to fetch or send data between client and server.',
            uz: 'API orqali mijoz va server o‘rtasida maʼlumot olish va yuborish mumkin.',
        },
        content: {
            ru: `В React часто используется API для получения данных с сервера. Самые популярные способы — это fetch и axios.

Запросы к API обычно выполняются внутри useEffect, чтобы сделать вызов при загрузке компонента. Также важно обрабатывать состояния загрузки и ошибок, чтобы улучшить пользовательский опыт.`,
            en: `In React, APIs are often used to fetch data from a server. Popular tools include fetch and axios.

API calls are typically made inside useEffect to trigger them on component mount. It’s also important to handle loading and error states for a better user experience.`,
            uz: `Reactda API yordamida serverdan maʼlumot olish keng qo‘llaniladi. Eng mashhur usullar — fetch va axios.

API chaqiruvlari odatda useEffect ichida bajariladi, bu komponent yuklanganda ishga tushadi. Yuklanish va xatolik holatlarini ham ko‘rsatish foydalanuvchiga qulaylik yaratadi.`,
        },
        exampleJsx: `
import React, { useEffect, useState } from 'react';

function Users() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/users')
      .then((res) => {
        if (!res.ok) throw new Error('Ошибка загрузки');
        return res.json();
      })
      .then((data) => setUsers(data))
      .catch((err) => setError(err.message))
      .finally(() => setLoading(false));
  }, []);

  if (loading) return <p>Загрузка...</p>;
  if (error) return <p>Ошибка: {error}</p>;

  return (
    <ul>
      {users.map((user) => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}

export default Users;`,
        exampleTsx: `
import React, { useEffect, useState } from 'react';

interface User {
  id: number;
  name: string;
}

const Users: React.FC = () => {
  const [users, setUsers] = useState<User[]>([]);
  const [loading, setLoading] = useState<boolean>(true);
  const [error, setError] = useState<string>('');

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/users')
      .then((res) => {
        if (!res.ok) throw new Error('Ошибка загрузки');
        return res.json();
      })
      .then((data) => setUsers(data))
      .catch((err) => setError(err.message))
      .finally(() => setLoading(false));
  }, []);

  if (loading) return <p>Загрузка...</p>;
  if (error) return <p>Ошибка: {error}</p>;

  return (
    <ul>
      {users.map((user) => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
};

export default Users;`,
        proTips: {
            ru: [
                'Используй useEffect для загрузки данных при монтировании.',
                'Обрабатывай ошибки и показывай пользователю сообщение.',
                'Подумай о состоянии загрузки, чтобы показать индикатор.',
            ],
            en: [
                'Use useEffect to load data when the component mounts.',
                'Handle errors and inform the user.',
                'Consider showing a loading indicator.',
            ],
            uz: [
                'Komponent yuklanganda maʼlumot olish uchun useEffect ishlating.',
                'Xatoliklarni ushlang va foydalanuvchiga bildiring.',
                'Yuklanish indikatorini ko‘rsatish haqida o‘ylang.',
            ],
        }
    }
    ,


    {
        key: 'react-query',
        title: {
            ru: 'Работа с React Query',
            en: 'Working with React Query',
            uz: 'React Query bilan ishlash',
        },
        description: {
            ru: 'React Query — это библиотека для управления серверным состоянием и запросами к API.',
            en: 'React Query is a library for managing server state and API requests.',
            uz: 'React Query — server holati va API so‘rovlarini boshqarish uchun kutubxona.',
        },
        content: {
            ru: `React Query позволяет легко выполнять запросы, кэшировать данные и управлять состоянием загрузки и ошибок. Она особенно полезна при частых обращениях к API и помогает избегать повторных запросов благодаря умному кэшированию.

С помощью useQuery ты можешь получить данные с сервера, а useMutation используется для отправки или обновления данных.`,
            en: `React Query makes it easy to fetch, cache, and manage server data and loading/error states. It is especially helpful for frequent API interactions and prevents redundant calls through smart caching.

You use useQuery to fetch data and useMutation to send or update data.`,
            uz: `React Query maʼlumotlarni olish, kechirish va yuklanish/xatolik holatlarini boshqarishni osonlashtiradi. U tez-tez API chaqiruvlari bo‘ladigan holatlarda juda foydali va ortiqcha so‘rovlarni oldini oladi.

Maʼlumot olish uchun useQuery, yuborish yoki yangilash uchun useMutation ishlatiladi.`,
        },
        exampleJsx: `
import React from 'react';
import { useQuery } from '@tanstack/react-query';

const fetchUsers = async () => {
  const res = await fetch('https://jsonplaceholder.typicode.com/users');
  if (!res.ok) throw new Error('Ошибка при загрузке');
  return res.json();
};

function Users() {
  const { data, isLoading, error } = useQuery(['users'], fetchUsers);

  if (isLoading) return <p>Загрузка...</p>;
  if (error) return <p>Ошибка: {(error as Error).message}</p>;

  return (
    <ul>
      {data.map((user) => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}

export default Users;`,
        exampleTsx: `
import React from 'react';
import { useQuery } from '@tanstack/react-query';

interface User {
  id: number;
  name: string;
}

const fetchUsers = async (): Promise<User[]> => {
  const res = await fetch('https://jsonplaceholder.typicode.com/users');
  if (!res.ok) throw new Error('Ошибка при загрузке');
  return res.json();
};

const Users: React.FC = () => {
  const { data, isLoading, error } = useQuery<User[], Error>(['users'], fetchUsers);

  if (isLoading) return <p>Загрузка...</p>;
  if (error) return <p>Ошибка: {error.message}</p>;

  return (
    <ul>
      {data?.map((user) => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
};

export default Users;`,
        proTips: {
            ru: [
                'React Query автоматически кэширует и обновляет данные.',
                'Ты можешь задать refetch-интервалы и поведение при фокусе.',
                'useMutation используется для POST, PUT, DELETE запросов.',
            ],
            en: [
                'React Query automatically caches and updates data.',
                'You can define refetch intervals and refetch on focus.',
                'useMutation is used for POST, PUT, DELETE requests.',
            ],
            uz: [
                'React Query avtomatik kechirish va yangilashni bajaradi.',
                'Refetch intervali va fokusda yangilanishni sozlash mumkin.',
                'POST, PUT, DELETE so‘rovlari uchun useMutation ishlatiladi.',
            ],
        },
    },


    {
        key: 'restAndGraphQL',
        title: {
            ru: 'REST и GraphQL',
            en: 'REST and GraphQL',
            uz: 'REST va GraphQL',
        },
        description: {
            ru: 'REST и GraphQL — это два подхода к работе с API. Каждый имеет свои особенности и преимущества.',
            en: 'REST and GraphQL are two different approaches to working with APIs. Each has its own strengths and characteristics.',
            uz: 'REST va GraphQL — API bilan ishlashning ikki xil yondashuvi bo‘lib, har birining o‘ziga xos afzalliklari bor.',
        },
        content: {
            ru: `REST — это архитектурный стиль, где у каждого ресурса есть свой URL, и для разных действий используются HTTP-методы (GET, POST, PUT, DELETE).

GraphQL — это язык запросов, который позволяет клиенту самому определять, какие данные он хочет получить. Это делает запросы гибкими и уменьшает объем лишней информации.

Оба подхода используются в реальных проектах, выбор зависит от задач, структуры данных и предпочтений команды.`,
            en: `REST is an architectural style where each resource has its own URL and different HTTP methods (GET, POST, PUT, DELETE) are used for actions.

GraphQL is a query language that allows the client to specify exactly what data it needs. This makes requests flexible and reduces over-fetching.

Both are widely used in real-world projects, and the choice depends on use case, data structure, and team preference.`,
            uz: `REST — bu har bir resurs uchun alohida URL bo‘lgan arxitektura uslubi bo‘lib, harakatlar uchun HTTP metodlari (GET, POST, PUT, DELETE) ishlatiladi.

GraphQL — bu mijozga kerakli maʼlumotlarni aniqlab berishga imkon beruvchi so‘rov tili. U orqali kerakli miqdordagi aniq maʼlumotlarni olish mumkin.

Ikkalasi ham amaliyotda keng qo‘llaniladi, tanlov esa vazifa, maʼlumotlar tuzilmasi va jamoa afzalliklariga bog‘liq.`,
        },
        exampleJsx: `
// Пример REST-запроса
fetch('https://jsonplaceholder.typicode.com/posts')
  .then((res) => res.json())
  .then((data) => console.log(data));

// Пример GraphQL-запроса
fetch('https://countries.trevorblades.com/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    query: '{ countries { name code } }',
  }),
})
  .then((res) => res.json())
  .then((data) => console.log(data));
`,
        exampleTsx: `
// Пример REST-запроса (TS)
interface Post {
  id: number;
  title: string;
}

fetch('https://jsonplaceholder.typicode.com/posts')
  .then((res) => res.json())
  .then((data: Post[]) => console.log(data));

// Пример GraphQL-запроса (TS)
interface Country {
  name: string;
  code: string;
}

fetch('https://countries.trevorblades.com/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    query: '{ countries { name code } }',
  }),
})
  .then((res) => res.json())
  .then((data: { data: { countries: Country[] } }) => console.log(data.data.countries));
`,
        proTips: {
            ru: [
                'REST проще для старта, используется повсеместно.',
                'GraphQL удобен при сложных и вложенных данных.',
                'GraphQL требует отдельного сервера и схемы.',
            ],
            en: [
                'REST is easier to start with and widely used.',
                'GraphQL is better for complex and nested data.',
                'GraphQL requires its own server and schema.',
            ],
            uz: [
                'RESTni boshlash oson va keng tarqalgan.',
                'GraphQL murakkab va ichki maʼlumotlar uchun qulay.',
                'GraphQL alohida server va sxemani talab qiladi.',
            ],
        },
    },


    {
        key: 'routing',
        title: {
            ru: 'Маршрутизация в React',
            en: 'Routing in React',
            uz: 'Reactda marshrutlash',
        },
        description: {
            ru: 'Маршрутизация позволяет пользователям перемещаться между страницами в приложении без перезагрузки.',
            en: 'Routing lets users navigate between pages in the app without reloading.',
            uz: 'Marshrutlash foydalanuvchiga sahifalar orasida sahifani yangilamasdan harakatlanish imkonini beradi.',
        },
        content: {
            ru: `Маршрутизация (routing) в React обычно реализуется с помощью библиотеки react-router-dom. Она позволяет организовать навигацию между компонентами на основе URL-адреса.

Можно настроить как простые маршруты (например, "/about"), так и вложенные маршруты и параметры (например, "/user/:id"). Компонент <BrowserRouter> оборачивает все приложение, а <Routes> и <Route> определяют, какие компоненты отображаются на каких путях.`,
            en: `Routing in React is usually implemented using the react-router-dom library. It allows navigation between components based on the URL.

You can set up simple routes (like "/about") as well as nested routes and parameters (like "/user/:id"). The <BrowserRouter> component wraps the app, and <Routes> and <Route> define which components appear for which paths.`,
            uz: `Reactda routing odatda react-router-dom kutubxonasi orqali amalga oshiriladi. Bu URL manzili asosida komponentlar orasida harakatlanishni taʼminlaydi.

Oddiy marshrutlar (masalan, "/about") va parametrlangan yoki ichki marshrutlar ("/user/:id") ham qo‘llab-quvvatlanadi. <BrowserRouter> butun ilovani o‘raydi, <Routes> va <Route> esa yo‘llarga komponentlarni biriktiradi.`,
        },
        exampleJsx: `
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </BrowserRouter>
  );
}
`,
        exampleTsx: `
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';

const App: React.FC = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </BrowserRouter>
  );
};
`,
        proTips: {
            ru: [
                'Используй useNavigate для программной навигации.',
                'Не забудь обернуть приложение в <BrowserRouter>.',
                'Для вложенной маршрутизации используй <Outlet>.',
            ],
            en: [
                'Use useNavigate for programmatic navigation.',
                'Don’t forget to wrap the app in <BrowserRouter>.',
                'Use <Outlet> for nested routing.',
            ],
            uz: [
                'Dasturiy navigatsiya uchun useNavigate dan foydalan.',
                'Ilovani <BrowserRouter> ichida o‘rashni unutmang.',
                'Ichki marshrutlar uchun <Outlet> ishlating.',
            ],
        },
    },


    {
        key: 'privateRouting',
        title: {
            ru: 'Приватные маршруты в React',
            en: 'Private Routes in React',
            uz: 'Reactda maxfiy yo‘llar (Private Routes)',
        },
        description: {
            ru: 'Приватные маршруты защищают страницы от неавторизованных пользователей, перенаправляя их на страницу входа.',
            en: 'Private routes protect pages from unauthorized users by redirecting them to the login page.',
            uz: 'Maxfiy yo‘llar ro‘yxatdan o‘tmagan foydalanuvchilarni login sahifasiga yo‘naltirib, ularni himoya qiladi.',
        },
        content: {
            ru: `Приватная маршрутизация (private routing) — это способ защитить определённые маршруты в приложении от неавторизованного доступа.

Это реализуется через компонент-обёртку, который проверяет, авторизован ли пользователь. Если нет — происходит редирект на страницу логина.

Типичный подход: хранить состояние авторизации в контексте или глобальном состоянии (например, Redux или Zustand).`,
            en: `Private routing is a way to protect certain routes in the app from unauthorized access.

It's implemented using a wrapper component that checks if the user is authenticated. If not, the user is redirected to the login page.

The usual method is to store auth state in a context or a global store like Redux or Zustand.`,
            uz: `Maxfiy marshrutlash (private routing) — bu baʼzi sahifalarni ro‘yxatdan o‘tmagan foydalanuvchilardan himoya qilish usuli.

Bu foydalanuvchining avtorizatsiya holatini tekshiruvchi o‘rash komponenti orqali amalga oshiriladi. Agar foydalanuvchi avtorizatsiya qilinmagan bo‘lsa, u login sahifasiga yo‘naltiriladi.

Ko‘pincha bu holat Context yoki global holatda (Redux, Zustand) saqlanadi.`,
        },
        exampleJsx: `
// PrivateRoute.jsx
import { Navigate } from 'react-router-dom';

function PrivateRoute({ children, isAuth }) {
  return isAuth ? children : <Navigate to="/login" />;
}

// App.jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Dashboard from './Dashboard';
import Login from './Login';
import PrivateRoute from './PrivateRoute';

function App() {
  const isAuthenticated = true; // замените на реальную проверку

  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<Login />} />
        <Route
          path="/dashboard"
          element={
            <PrivateRoute isAuth={isAuthenticated}>
              <Dashboard />
            </PrivateRoute>
          }
        />
      </Routes>
    </BrowserRouter>
  );
}
`,
        exampleTsx: `
// PrivateRoute.tsx
import { Navigate } from 'react-router-dom';

interface Props {
  children: React.ReactNode;
  isAuth: boolean;
}

const PrivateRoute: React.FC<Props> = ({ children, isAuth }) => {
  return isAuth ? <>{children}</> : <Navigate to="/login" />;
};

// App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Dashboard from './Dashboard';
import Login from './Login';
import PrivateRoute from './PrivateRoute';

const App: React.FC = () => {
  const isAuthenticated = true; // замените на реальную проверку

  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<Login />} />
        <Route
          path="/dashboard"
          element={
            <PrivateRoute isAuth={isAuthenticated}>
              <Dashboard />
            </PrivateRoute>
          }
        />
      </Routes>
    </BrowserRouter>
  );
};
`,
        proTips: {
            ru: [
                'Держи авторизацию в Context или глобальном store.',
                'Добавь спиннер, если авторизация ещё проверяется.',
                'Не показывай защищённые компоненты до проверки isAuth.',
            ],
            en: [
                'Keep auth state in Context or global store.',
                'Add a loading spinner while checking auth status.',
                'Do not render protected components until isAuth is verified.',
            ],
            uz: [
                'Avtorizatsiya holatini Context yoki global store’da saqla.',
                'Tekshiruv davomida yuklanish indikatorini ko‘rsat.',
                'isAuth tekshirilmaguncha maxfiy sahifalarni render qilma.',
            ],
        },
    },


    {
        key: 'tailwind',
        title: {
            ru: 'Tailwind CSS в React',
            en: 'Tailwind CSS in React',
            uz: 'Reactda Tailwind CSS',
        },
        description: {
            ru: 'Tailwind — это утилитарный CSS-фреймворк, позволяющий быстро и гибко стилизовать компоненты прямо в JSX.',
            en: 'Tailwind is a utility-first CSS framework that allows fast and flexible styling directly in JSX.',
            uz: 'Tailwind — bu JSX ichida tez va moslashuvchan uslub berish imkonini beruvchi utility-first CSS framework.',
        },
        content: {
            ru: `Tailwind CSS — это фреймворк, основанный на утилитарных классах. Вместо написания кастомного CSS ты применяешь классы прямо в JSX, получая быстрый и адаптивный дизайн.

Tailwind идеально подходит для React, так как позволяет быстро стилизовать компоненты без переключения контекста.

Подключение:
1. Установи Tailwind через PostCSS/Vite/CRA.
2. Настрой \`tailwind.config.js\` (например, добавь кастомные цвета, breakpoints и т.д.).
3. Используй классы в JSX: \`<div className="bg-blue-500 text-white p-4 rounded-xl">\` и т.д.`,
            en: `Tailwind CSS is a utility-first framework. Instead of writing custom CSS, you apply predefined classes directly in JSX, achieving fast and responsive design.

It’s perfect for React because it lets you style components without context switching.

Setup:
1. Install Tailwind via PostCSS/Vite/CRA.
2. Configure \`tailwind.config.js\` (add custom colors, breakpoints, etc).
3. Use classes in JSX: \`<div className="bg-blue-500 text-white p-4 rounded-xl">\`.`,
            uz: `Tailwind CSS — bu utility-first framework bo‘lib, siz alohida CSS yozmasdan, tayyor sinflarni JSX da ishlatishingiz mumkin, bu esa tez va moslashuvchan dizayn yaratadi.

Tailwind React uchun juda mos keladi — uslub berishda kontekst almashtirishga hojat yo‘q.

Ulash:
1. Tailwind’ni PostCSS/Vite/CRA orqali o‘rnat.
2. \`tailwind.config.js\` faylini sozla (masalan, ranglar, breakpoint’lar).
3. JSX ichida sinflarni ishlat: \`<div className="bg-blue-500 text-white p-4 rounded-xl">\`.`,
        },
        exampleJsx: `
// Card.jsx
export default function Card() {
  return (
    <div className="bg-white shadow-md rounded-2xl p-6 max-w-sm">
      <h2 className="text-2xl font-bold mb-2 text-gray-800">Заголовок</h2>
      <p className="text-gray-600">Описание карточки с использованием Tailwind CSS.</p>
    </div>
  );
}
`,
        exampleTsx: `
// Card.tsx
import React from 'react';

const Card: React.FC = () => {
  return (
    <div className="bg-white shadow-md rounded-2xl p-6 max-w-sm">
      <h2 className="text-2xl font-bold mb-2 text-gray-800">Заголовок</h2>
      <p className="text-gray-600">Описание карточки с использованием Tailwind CSS.</p>
    </div>
  );
};

export default Card;
`,
        proTips: {
            ru: [
                'Планируй структуру классов — избегай слишком длинных строк.',
                'Используй `@apply` в CSS-модулях для переиспользования стилей.',
                'Настраивай `tailwind.config.js` под нужды проекта (цвета, шрифты и т.д.).',
            ],
            en: [
                'Plan class structure — avoid overly long class strings.',
                'Use `@apply` in CSS modules to reuse styles.',
                'Customize `tailwind.config.js` for project needs (colors, fonts, etc).',
            ],
            uz: [
                'Classlarni tartibli rejalashtir — juda uzun class qatorlaridan saqlan.',
                '`@apply` yordamida CSS modullarida uslublarni qayta ishlat.',
                '`tailwind.config.js` faylini loyiha ehtiyojlariga moslashtir (ranglar, shriftlar).',
            ],
        },
    },


    {
        key: 'styledComponents',
        title: {
            ru: 'Styled Components в React',
            en: 'Styled Components in React',
            uz: 'Reactda Styled Components',
        },
        description: {
            ru: 'Styled Components позволяет писать CSS прямо в JavaScript, создавая изолированные и переиспользуемые стили для компонентов.',
            en: 'Styled Components allows writing CSS directly in JavaScript, creating isolated and reusable component styles.',
            uz: 'Styled Components — bu komponentlar uchun izolyatsiyalangan va qayta ishlatiladigan uslublarni JS ichida yozish imkonini beradi.',
        },
        content: {
            ru: `Styled Components — это библиотека для стилизации компонентов в React, используя синтаксис tagged template literals. Она позволяет создавать полностью изолированные стили, привязанные к конкретному компоненту.

Основные преимущества:
- Локальность стилей: стили не "утекают" за пределы компонента.
- Динамические стили через props.
- Нет необходимости в отдельных CSS-файлах.

Установка:
\`npm install styled-components\`

Пример:
Создаём стилизованный компонент \`Button\`, который можно использовать как обычный React-компонент.`,
            en: `Styled Components is a library for styling React components using tagged template literals. It enables fully scoped styles tied to a specific component.

Benefits:
- Local styles that don't leak outside.
- Dynamic styling using props.
- No need for separate CSS files.

Install:
\`npm install styled-components\`

Example:
Create a styled \`Button\` component and use it like a regular React component.`,
            uz: `Styled Components — bu React komponentlarini tagged template literal orqali uslublash uchun kutubxona. U komponentga biriktirilgan mustaqil uslublarni yaratish imkonini beradi.

Afzalliklari:
- Uslublar faqat o‘z komponentiga tegishli bo‘ladi.
- Props orqali dinamik uslub berish mumkin.
- CSS fayllar kerak emas.

O‘rnatish:
\`npm install styled-components\`

Misol:
Styled \`Button\` komponenti yaratib, uni oddiy React komponenti sifatida ishlating.`,
        },
        exampleJsx: `
import styled from 'styled-components';

const Button = styled.button\`
  background-color: #4f46e5;
  color: white;
  padding: 12px 24px;
  border-radius: 8px;
  font-size: 16px;
  border: none;

  &:hover {
    background-color: #4338ca;
  }
\`;

export default function App() {
  return <Button>Нажми меня</Button>;
}
`,
        exampleTsx: `
import styled from 'styled-components';

const Button = styled.button\`
  background-color: #4f46e5;
  color: white;
  padding: 12px 24px;
  border-radius: 8px;
  font-size: 16px;
  border: none;

  &:hover {
    background-color: #4338ca;
  }
\`;

const App: React.FC = () => {
  return <Button>Нажми меня</Button>;
};

export default App;
`,
        proTips: {
            ru: [
                'Переименовывай компоненты для читаемости: `const MyCard = styled.div`.',
                'Используй props внутри шаблона: `color: \${props => props.primary ? "blue" : "gray"}`.',
                'Избегай глубоких вложенностей в стилях — они усложняют поддержку.',
            ],
            en: [
                'Rename styled components for clarity: `const MyCard = styled.div`.',
                'Use props in styles: `color: \${props => props.primary ? "blue" : "gray"}`.',
                'Avoid deep nested styles — they reduce maintainability.',
            ],
            uz: [
                'Styled komponentlarga tushunarli nom ber: `const MyCard = styled.div`.',
                'Uslublarda propslardan foydalan: `color: \${props => props.primary ? "blue" : "gray"}`.',
                'Qattiq chuqurlikdagi ichki uslublardan saqlaning — ularni boshqarish qiyin bo‘ladi.',
            ],
        },
    },


    {
        key: 'render',
        title: {
            ru: 'Условный рендеринг в React',
            en: 'Conditional Rendering in React',
            uz: 'React’da shartli rendering',
        },
        description: {
            ru: 'Условный рендеринг позволяет показывать разные элементы в зависимости от условий. Это основа динамического интерфейса в React.',
            en: 'Conditional rendering lets you show different elements depending on conditions. It’s the basis of dynamic UI in React.',
            uz: 'Shartli rendering — foydalanuvchi holatiga qarab turli elementlarni ko‘rsatishga imkon beradi. React’dagi dinamik UI asosi.',
        },
        content: {
            ru: `Условный рендеринг в React используется, когда нужно отобразить компонент или элемент в зависимости от условий. Это может быть проверка авторизации, состояния загрузки и т.д.

Способы условного рендеринга:
1. Оператор \`if\` или тернарный \`? :\`
2. Логическое И \`&&\`
3. Возврат \`null\` (если не нужно ничего рендерить)

Пример с тернарным оператором:
Если пользователь авторизован — показать приветствие, иначе кнопку входа.`,
            en: `Conditional rendering in React is used to display elements or components based on certain conditions (e.g., user is logged in, loading status).

Ways to render conditionally:
1. \`if\` or ternary \`? :\`
2. Logical AND \`&&\`
3. Return \`null\` if nothing should be shown

Example with ternary:
Show greeting if the user is logged in, otherwise show login button.`,
            uz: `React’da shartli rendering element yoki komponentni holatga qarab ko‘rsatish uchun ishlatiladi (masalan, foydalanuvchi tizimga kirganmi, yuklanmoqda va hokazo).

Shartli rendering usullari:
1. \`if\` yoki ternary \`? :\`
2. Mantiqiy \`&&\`
3. \`null\` qaytarish (agar hech narsa ko‘rsatilmasa)

Misol:
Agar foydalanuvchi tizimga kirgan bo‘lsa — salomlashuv, aks holda login tugmasi.`,
        },
        exampleJsx: `
import React from 'react';

export default function App() {
  const isLoggedIn = true;

  return (
    <div>
      {isLoggedIn ? <h2>Добро пожаловать!</h2> : <button>Войти</button>}
    </div>
  );
}
`,
        exampleTsx: `
import React from 'react';

const App: React.FC = () => {
  const isLoggedIn: boolean = true;

  return (
    <div>
      {isLoggedIn ? <h2>Добро пожаловать!</h2> : <button>Войти</button>}
    </div>
  );
};

export default App;
`,
        proTips: {
            ru: [
                'Старайся не писать сложную логику прямо внутри JSX, выноси её в переменные.',
                'Если не нужно ничего рендерить — возвращай `null`.',
                'Логическое `&&` удобно для простых условий: `isVisible && <Component />`.',
            ],
            en: [
                'Avoid writing complex logic directly in JSX — extract it to variables.',
                'Return `null` if no rendering is needed.',
                '`&&` is useful for simple conditions: `isVisible && <Component />`.',
            ],
            uz: [
                'Murakkab mantiqni JSX ichida yozishdan saqlaning — alohida o‘zgaruvchiga ajrating.',
                'Hech narsa ko‘rsatish kerak bo‘lmasa `null` qaytaring.',
                'Oddiy shartlar uchun `&&` ishlatish qulay: `isVisible && <Component />`.',
            ],
        },
    },


    {
        key: 'lifting-state',
        title: {
            ru: 'Подъём состояния (Lifting State Up) в React',
            en: 'Lifting State Up in React',
            uz: 'React’da state’ni yuqoriga ko‘tarish (Lifting State Up)',
        },
        description: {
            ru: 'Подъём состояния позволяет делиться состоянием между несколькими компонентами путём перемещения его в ближайший общий родительский компонент.',
            en: 'Lifting state up allows sharing state between components by moving it to their closest common parent.',
            uz: 'State’ni yuqoriga ko‘tarish — bir nechta komponent o‘rtasida ma’lumot almashish uchun uni umumiy ota komponentga ko‘chirishdir.',
        },
        content: {
            ru: `Когда два дочерних компонента нуждаются в доступе к одному и тому же состоянию или должны синхронизироваться, лучше всего "поднять" это состояние в их общего родителя. Это называется Lifting State Up.

Вместо того чтобы каждый компонент хранил своё состояние, общее состояние хранится в родителе, а дочерние получают данные и функции через пропсы.

Этот подход делает поведение приложения более предсказуемым и уменьшает дублирование логики.`,
            en: `When two child components need to access or sync shared data, it's best to "lift" the state up to their common parent — this is called Lifting State Up.

Instead of each component managing its own state, the parent holds the shared state and passes it down as props.

This makes your app’s logic more predictable and avoids duplication.`,
            uz: `Agar ikki yoki undan ko‘p komponent bir xil ma’lumotdan foydalanishi kerak bo‘lsa, bu ma’lumotni umumiy ota komponentga ko‘chirish kerak — bu "Lifting State Up" deb ataladi.

Har bir komponent o‘z state’ini saqlamasdan, ota komponent umumiy state’ni boshqaradi va uni props orqali uzatadi.

Bu usul ilovaning mantiqiy tuzilishini soddalashtiradi va kodni takrorlashdan saqlaydi.`,
        },
        exampleJsx: `
import React, { useState } from 'react';

function Parent() {
  const [value, setValue] = useState('');

  return (
    <div>
      <Input value={value} onChange={setValue} />
      <Display value={value} />
    </div>
  );
}

function Input({ value, onChange }) {
  return (
    <input
      type="text"
      value={value}
      onChange={(e) => onChange(e.target.value)}
    />
  );
}

function Display({ value }) {
  return <p>Вы ввели: {value}</p>;
}

export default Parent;
`,
        exampleTsx: `
import React from 'react';

type InputProps = {
  value: string;
  onChange: (val: string) => void;
};

type DisplayProps = {
  value: string;
};

const Input: React.FC<InputProps> = ({ value, onChange }) => (
  <input
    type="text"
    value={value}
    onChange={(e) => onChange(e.target.value)}
  />
);

const Display: React.FC<DisplayProps> = ({ value }) => (
  <p>Вы ввели: {value}</p>
);

const Parent: React.FC = () => {
  const [value, setValue] = React.useState('');

  return (
    <div>
      <Input value={value} onChange={setValue} />
      <Display value={value} />
    </div>
  );
};

export default Parent;
`,
        proTips: {
            ru: [
                'Подъём состояния облегчает управление общими данными между компонентами.',
                'Если компонентов слишком много и они тесно связаны — используй Context.',
                'Передавай как само значение, так и функции для изменения состояния.',
            ],
            en: [
                'Lifting state helps coordinate shared data across components.',
                'If too many components depend on the same data — consider using Context.',
                'Pass both value and its update function as props.',
            ],
            uz: [
                'State’ni yuqoriga ko‘tarish komponentlar o‘rtasida ma’lumot almashishni osonlashtiradi.',
                'Agar komponentlar juda ko‘p va ularning bog‘liqligi yuqori bo‘lsa — Context’dan foydalan.',
                'Props orqali qiymatni ham, uni o‘zgartiruvchi funksiyani ham uzat.',
            ],
        },
    },


    {
        key: 'lazyload',
        title: {
            ru: 'Ленивая загрузка компонентов (Lazy Loading)',
            en: 'Lazy Loading Components in React',
            uz: 'Komponentlarni kechikkan yuklash (Lazy Loading)',
        },
        description: {
            ru: 'Lazy loading позволяет загружать компоненты React только при необходимости, уменьшая первоначальный объём загрузки и улучшая производительность.',
            en: 'Lazy loading lets you load React components only when needed, reducing initial bundle size and improving performance.',
            uz: 'Lazy loading faqat kerak bo‘lganida komponentlarni yuklaydi, bu boshlang‘ich yuklama hajmini kamaytiradi va samaradorlikni oshiradi.',
        },
        content: {
            ru: `Ленивая загрузка (Lazy Loading) — это приём оптимизации, при котором части интерфейса или модули приложения загружаются только тогда, когда они действительно нужны. В React это особенно полезно для больших компонентов или страниц, к которым пользователь может не перейти сразу.

React предоставляет функцию \`React.lazy()\` для динамического импорта компонентов. Вместе с \`Suspense\` можно отображать загрузочный индикатор, пока компонент загружается.`,
            en: `Lazy loading is an optimization strategy where parts of the interface or modules are loaded only when they're actually needed. In React, it's especially useful for large components or routes that may not be accessed immediately.

React provides \`React.lazy()\` for dynamic imports. Combine it with \`Suspense\` to show a fallback UI while the component is loading.`,
            uz: `Lazy loading — bu optimallashtirish usuli bo‘lib, UI qismlari yoki modullar faqat kerak bo‘lganda yuklanadi. React’da bu katta komponentlar yoki sahifalar uchun ayniqsa foydali.

React’da \`React.lazy()\` yordamida dinamik import qilish mumkin. Yuklanish vaqtida \`Suspense\` orqali zaxira UI (masalan, yuklanmoqda belgisi) ko‘rsatiladi.`,
        },
        exampleJsx: `
import React, { Suspense } from 'react';

const HeavyComponent = React.lazy(() => import('./HeavyComponent'));

export default function App() {
  return (
    <div>
      <h1>Добро пожаловать</h1>
      <Suspense fallback={<p>Загрузка...</p>}>
        <HeavyComponent />
      </Suspense>
    </div>
  );
}
`,
        exampleTsx: `
import React, { Suspense } from 'react';

const HeavyComponent = React.lazy(() => import('./HeavyComponent'));

const App: React.FC = () => {
  return (
    <div>
      <h1>Добро пожаловать</h1>
      <Suspense fallback={<p>Загрузка...</p>}>
        <HeavyComponent />
      </Suspense>
    </div>
  );
};

export default App;
`,
        proTips: {
            ru: [
                'Lazy loading особенно полезен для маршрутов и тяжёлых компонентов.',
                'Оборачивай ленивые компоненты в `<Suspense>`, чтобы избежать ошибок загрузки.',
                'Не злоупотребляй — ленивая загрузка добавляет задержку при первом отображении компонента.',
            ],
            en: [
                'Lazy loading is especially useful for routes and heavy components.',
                'Wrap lazy components in `<Suspense>` to handle loading gracefully.',
                'Avoid overusing lazy loading — it adds delay on first render.',
            ],
            uz: [
                'Lazy loading yo‘llar (routes) va og‘ir komponentlar uchun juda foydali.',
                'Lazily yuklanadigan komponentlarni `<Suspense>` bilan o‘rash kerak.',
                'Har joyda ishlatma — bu birinchi yuklashda kechikishga olib keladi.',
            ],
        },
    },
];